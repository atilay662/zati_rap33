projection;
strict ( 2 );
use side effects;

define behavior for ZC_ATI_AKVBAS //alias <alias_name>
use etag
{
  use create;
  use update;
  use delete;
  use action copyActivity;
  use action acceptAct;
  use action rejectAct;
  use action calcPrice;
  use association _akvkal { create; }
}

define behavior for ZC_ATI_AKVKAL //alias <alias_name>
{
  use update;
  use delete;

  use association _akvbas;
}